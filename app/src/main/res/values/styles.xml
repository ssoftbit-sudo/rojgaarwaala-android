<resources>
    <style name="ThemeOverlay.App.ProgressBarWhite" parent="">
        <item name="android:colorAccent">@color/white</item>
        <item name="android:indeterminateTint">@color/white</item>
    </style>

    <!-- Pill search fields (Home / Stories / Images) -->
    <style name="Widget.Rojgaarwaala.EditText.SearchField" parent="Widget.AppCompat.EditText">
        <item name="android:background">@null</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textColorHint">@color/search_hint</item>
        <item name="android:textSize">15sp</item>
    </style>
</resources> 